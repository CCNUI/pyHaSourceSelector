# Configuration for PySourceSelector
# Rename this file to .env and modify the values as needed.

# Timeout in seconds for each network request.
# A lower value makes the test finish faster but might incorrectly fail slow servers.
# A higher value is more reliable but the test takes longer.
REQUEST_TIMEOUT=5